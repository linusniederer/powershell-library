class Documentation {

    [string] $docPath

    Documentation ( $path, [array] $files, [array] $folders ) {
        
        $this.docPath = $path

        foreach ( $file in $files ) {

        }
    }


}